import Golden.Basic

/-!
The golden fixture's root module. A submission's root carries no annotation,
so this module doc is plain prose: it proves the inspector reports a module
doc with `hasFrontmatter: false` and no frontmatter error.
-/
